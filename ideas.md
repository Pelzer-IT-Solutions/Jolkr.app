## Jolkr.app 14-05-2026

- [ ] getUsersBatch (/users/batch BE-endpoint active, FE does per-id getUser)
- [ ] discoverServers (public-server-discovery UI is not build yet)
- [ ] search

- [x] group contextmenu (leave groupe, mute, etc)
- [x] group info (same as view profile, only with the group members etc)
- [x] group owner settings model (change group name etc)

- [x] pinned messages needed to be clickable and that when clicked it scrolls to that message (rest needs to stay the same)
- [x] shared files needed to be clickable and that when clicked it scrolls to that message (rest needs to stay the same)

- [ ] full directory and plan / audit / memory / todo cleanup. everything that is obsolete can be cleaned up. "plan-ha-cluster.md" can stay

- [ ] create one full single doc that can be read by any human and loaded by any AI so both know exactly how to work on Jolkr and what all the api endpoints are and do. and how the app is functioning now. it also needs to explain

- [x] Make the landing page the same colors as the default app colors. (light/dark and system) make the landing page default use system unless user otherwise choses, then store in localStorage so it can be used when the user revisits the website. 