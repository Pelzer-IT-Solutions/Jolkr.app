// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { Category } from "./Category";
import type { Channel } from "./Channel";
import type { ChannelOverwrite } from "./ChannelOverwrite";
import type { DmChannel } from "./DmChannel";
import type { Friendship } from "./Friendship";
import type { FriendshipUpdateKind } from "./FriendshipUpdateKind";
import type { GifFavorite } from "./GifFavorite";
import type { Message } from "./Message";
import type { NotificationSettingPayload } from "./NotificationSettingPayload";
import type { Reaction } from "./Reaction";
import type { Role } from "./Role";
import type { Server } from "./Server";
import type { Thread } from "./Thread";

/**
 * Events sent FROM the server TO the client over the WebSocket.
 */
export type WsEvent = { "op": "Ready", "d": { user_id: string, session_id: string, } } | { "op": "HeartbeatAck", "d": { seq: number, } } | { "op": "MessageCreate", "d": { message: Message, } } | { "op": "MessageUpdate", "d": { message: Message, } } | { "op": "MessageDelete", "d": { message_id: string, channel_id: string, } } | { "op": "TypingStart", "d": { channel_id: string, user_id: string, timestamp: number, } } | { "op": "PresenceUpdate", "d": { user_id: string, status: string, } } | { "op": "DmCreate", "d": { channel: DmChannel, } } | { "op": "DmUpdate", "d": { channel: DmChannel, } } | { "op": "DmClose", "d": { dm_id: string, } } | { "op": "DmMessageHide", "d": { dm_id: string, message_id: string, } } | { "op": "ThreadCreate", "d": { thread: Thread, } } | { "op": "ThreadUpdate", "d": { thread: Thread, } } | { "op": "ChannelCreate", "d": { channel: Channel, } } | { "op": "ChannelUpdate", "d": { channel: Channel, } } | { "op": "ChannelDelete", "d": { channel_id: string, server_id: string, } } | { "op": "CategoryCreate", "d": { category: Category, } } | { "op": "CategoryUpdate", "d": { category: Category, } } | { "op": "CategoryDelete", "d": { category_id: string, server_id: string, } } | { "op": "MemberJoin", "d": { server_id: string, user_id: string, } } | { "op": "MemberLeave", "d": { server_id: string, user_id: string, } } | { "op": "ServerUpdate", "d": { server: Server, } } | { "op": "ServerDelete", "d": { server_id: string, } } | { "op": "MemberUpdate", "d": { 
/**
 * Owning server identifier.
 */
server_id: string, 
/**
 * Affected user identifier.
 */
user_id: string, 
/**
 * New timeout expiry (RFC3339); `None` is omitted on the wire.
 */
timeout_until?: string, 
/**
 * New nickname; `None` is omitted, empty string clears.
 */
nickname?: string, 
/**
 * New full set of role IDs the member holds.
 */
role_ids?: Array<string>, } } | { "op": "RoleCreate", "d": { 
/**
 * Owning server identifier.
 */
server_id: string, 
/**
 * Created role.
 */
role: Role, } } | { "op": "RoleUpdate", "d": { 
/**
 * Owning server identifier.
 */
server_id: string, 
/**
 * Updated role.
 */
role: Role, } } | { "op": "RoleDelete", "d": { 
/**
 * Owning server identifier.
 */
server_id: string, 
/**
 * Deleted role identifier.
 */
role_id: string, } } | { "op": "ChannelPermissionUpdate", "d": { 
/**
 * Affected channel identifier.
 */
channel_id: string, 
/**
 * Owning server identifier.
 */
server_id: string, 
/**
 * Full set of overwrites currently on the channel.
 */
overwrites: Array<ChannelOverwrite>, } } | { "op": "ReactionUpdate", "d": { channel_id: string, message_id: string, reactions: Array<Reaction>, } } | { "op": "PollUpdate", "d": { poll: import("./Poll").Poll, channel_id: string, message_id: string, } } | { "op": "DmCallRing", "d": { dm_id: string, caller_id: string, caller_username: string, 
/**
 * `true` if the caller is starting a video call, `false` for voice-only.
 */
is_video: boolean, } } | { "op": "DmCallAccept", "d": { dm_id: string, user_id: string, } } | { "op": "DmCallReject", "d": { dm_id: string, user_id: string, } } | { "op": "DmCallEnd", "d": { dm_id: string, user_id: string, } } | { "op": "DmMessagesRead", "d": { dm_id: string, user_id: string, message_id: string, } } | { "op": "ChannelMessagesRead", "d": { channel_id: string, user_id: string, message_id: string, } } | { "op": "ServerMessagesRead", "d": { server_id: string, user_id: string, } } | { "op": "UserUpdate", "d": { user_id: string, status: string | null, display_name: string | null, avatar_url: string | null, bio: string | null, 
/**
 * Profile banner / accent color. Visible to peers in profile cards
 * and member rows, so it must be broadcast on change too.
 */
banner_color?: string, 
/**
 * Self-only privacy preference — included so the user's other tabs
 * reflect a settings toggle without a refresh. Other users ignore it.
 */
show_read_receipts?: boolean, 
/**
 * Self-only privacy preference — DM filter (`all` | `friends` | `none`).
 * Sent only on the user's own user-channel, so other peers ignore it.
 */
dm_filter?: string, 
/**
 * Self-only privacy preference — whether others can send friend requests.
 * Sent only on the user's own user-channel, so other peers ignore it.
 */
allow_friend_requests?: boolean, 
/**
 * Self-only preference — UI language (BCP-47 lite). Sent only on the
 * user's own user-channel so all their sessions converge on the same
 * locale after a Settings change.
 */
preferred_language?: string, } } | { "op": "EmailVerified", "d": { user_id: string, } } | { "op": "GifFavoriteUpdate", "d": { 
/**
 * Populated when the change is an add. None means a remove.
 */
added?: GifFavorite, 
/**
 * Populated when the change is a remove. None means an add.
 */
removed_gif_id?: string, } } | { "op": "UserCallPresence", "d": { 
/**
 * DM call room id (mutually exclusive with `channel_id`).
 */
dm_id: string | null, 
/**
 * Server voice channel id (mutually exclusive with `dm_id`).
 */
channel_id: string | null, is_video: boolean | null, } } | { "op": "FriendshipUpdate", "d": { friendship: Friendship, kind: FriendshipUpdateKind, } } | { "op": "NotificationSettingUpdate", "d": { target_type: string, target_id: string, setting?: NotificationSettingPayload, } } | { "op": "Error", "d": { message: string, } };
