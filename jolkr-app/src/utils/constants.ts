/**
 * Project-wide constants shared across pages and components.
 *
 * Add values here when they need to stay in sync between the UI and the
 * backend (e.g. password rules, attachment caps), or when they are referenced
 * from more than one component.
 */

/** Minimum password length enforced by both the UI and the backend's
 *  /auth/reset-password-confirm + /auth/register routes. */
export const MIN_PASSWORD_LENGTH = 8;
